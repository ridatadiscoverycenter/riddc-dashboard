//import { fish } from '@/utils/erddap/db';
//import FishLite from './FishLite';
//import SpeciesAbundanceChart from './SpeciesAbundanceChart';

export default async function Fish() {
  //const samples = await fish.fetchSamples();
  //const speciesSamples = await fetchSpecies();
  //<FishLite data={samples.map(({ abun, species }) => ({ a: species, b: abun }))} />
  //<SpeciesAbundanceChart data={samples} species="cunner" />
  return <div>Fish</div>;
}
